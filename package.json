{
  "name": "openai-chatbot",
  "version": "1.0.0",
  "description": "An AI chatbot web application developed with the MERN stack: **MongoDB**, **Express**, **React** and **Node**, using the [OpenAI API](https://openai.com/blog/openai-api). See all packages and libraries used [here](#Frontend-Packages-and-Libraries).",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
    "start": "npm run dev & wait-on http://localhost:5173 && open http://localhost:5173",
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run dev"
  },
  "author": "Artt2",
  "license": "MIT",
  "dependencies": {
    "wait-on": "^7.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
